kind: Cluster

apiVersion: kind.x-k8s.io/v1alpha4

nodes: 
  - role: control-plane
    extraPortMappings:
      - containerPort: 30007
        hostPort:  8080
      - containerPort: 30008
        hostPort: 8443
  - role: worker
  - role: worker

# kubectl label node judge0-cluster-worker node-role.kubernetes.io/worker=worker
# kubectl label node judge0-cluster-worker2 node-role.kubernetes.io/worker=worker
